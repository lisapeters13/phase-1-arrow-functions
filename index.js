let divide = function() {
    return 2000 / 100;
  };
  
  let result = divide();
  console.log(result); // Output: 20

  const square = (num) => num * num;

  const add = (num1, num2) => num1 + num2;